<!doctype html>
<html lang="vi">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Quiz Ôn Luyện</title>
  <style>
    body {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      height: 100%;
      width: 100%;
    }
    
    html {
      height: 100%;
      width: 100%;
    }

    .app-wrapper {
      width: 100%;
      height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .quiz-container {
      background: white;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      width: 100%;
      max-width: 1200px;
      height: 90%;
      max-height: 700px;
      display: flex;
      overflow: hidden;
    }

    /* Ph���n câu hỏi bên trái */
    .question-section {
      flex: 1;
      display: flex;
      flex-direction: column;
      padding: 40px;
      overflow-y: auto;
    }

    .quiz-header {
      margin-bottom: 30px;
      border-bottom: 3px solid #667eea;
      padding-bottom: 20px;
    }

    .quiz-header h1 {
      color: #333;
      font-size: 28px;
      margin: 0 0 10px 0;
    }

    .quiz-header p {
      color: #666;
      font-size: 14px;
      margin: 0;
    }

    .question-display {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .question-card {
      background: #f8f9fa;
      border-radius: 12px;
      padding: 30px;
      border-left: 5px solid #667eea;
      flex: 1;
      display: flex;
      flex-direction: column;
      animation: slideIn 0.5s cubic-bezier(0.4, 0, 0.2, 1);
      opacity: 1;
      transition: opacity 0.3s ease-out, transform 0.3s ease-out;
    }

    .question-card.fade-out {
      opacity: 0;
      transform: translateX(-20px);
    }

    @keyframes slideIn {
      from {
        opacity: 0;
        transform: translateX(30px);
      }
      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    .question-number {
      font-size: 14px;
      color: #667eea;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .question-title {
      font-size: 22px;
      font-weight: 600;
      color: #333;
      margin-bottom: 25px;
    }

    .options-list {
      list-style: none;
      padding: 0;
      margin: 0 0 20px 0;
    }

    .option-item {
      margin-bottom: 12px;
    }

    .option-label {
      display: flex;
      align-items: center;
      padding: 15px 18px;
      background: white;
      border: 2px solid #e0e0e0;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .option-label:hover {
      border-color: #667eea;
      background: #f0f4ff;
      transform: translateX(5px);
    }

    .option-label.option-correct {
      border-color: #28a745;
      background: #d4edda;
    }

    .option-label.option-incorrect {
      border-color: #dc3545;
      background: #f8d7da;
    }

    .option-label input[type="radio"] {
      margin-right: 15px;
      width: 20px;
      height: 20px;
      cursor: pointer;
      accent-color: #667eea;
    }

    .option-label input[type="radio"]:disabled {
      cursor: not-allowed;
    }

    .answer-feedback {
      margin-top: 20px;
      padding: 15px;
      border-radius: 8px;
      font-size: 15px;
    }

    .feedback-correct {
      background: #d4edda;
      color: #155724;
      border: 2px solid #28a745;
    }

    .feedback-incorrect {
      background: #f8d7da;
      color: #721c24;
      border: 2px solid #dc3545;
    }

    .option-text {
      font-size: 16px;
      color: #333;
      flex: 1;
    }

    .navigation-buttons {
      display: flex;
      gap: 15px;
      margin-top: 20px;
    }

    .btn {
      padding: 12px 24px;
      font-size: 16px;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .btn:disabled {
      opacity: 0.5;
      cursor: not-allowed;
    }

    .btn-nav {
      background: #667eea;
      color: white;
      flex: 1;
    }

    .btn-nav:hover:not(:disabled) {
      background: #5568d3;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    .btn-submit {
      background: #27ae60;
      color: white;
      width: 100%;
    }

    .btn-submit:hover {
      background: #229954;
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(39, 174, 96, 0.4);
    }

    /* Phần danh sách câu hỏi bên phải */
    .sidebar {
      width: 280px;
      background: #f8f9fa;
      border-left: 1px solid #e0e0e0;
      padding: 30px 20px;
      overflow-y: auto;
      display: flex;
      flex-direction: column;
    }

    .sidebar-header {
      font-size: 18px;
      font-weight: 700;
      color: #333;
      margin-bottom: 20px;
      text-align: center;
    }

    .question-list {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 10px;
      margin-bottom: 20px;
    }

    .question-item {
      aspect-ratio: 1;
      border: 2px solid #e0e0e0;
      border-radius: 8px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 16px;
      font-weight: 600;
      color: #666;
      cursor: pointer;
      transition: all 0.3s ease;
      background: white;
      position: relative;
    }

    .question-item:hover {
      border-color: #667eea;
      transform: scale(1.05);
    }

    .question-item.active {
      background: #667eea;
      color: white;
      border-color: #667eea;
    }

    .question-item.answered {
      background: #d4edda;
      border-color: #28a745;
      color: #155724;
    }

    .question-item.answered::after {
      content: '✓';
      position: absolute;
      top: 2px;
      right: 4px;
      font-size: 12px;
      color: #28a745;
    }

    .question-item.correct {
      background: #d4edda;
      border-color: #28a745;
      color: #155724;
    }

    .question-item.incorrect {
      background: #f8d7da;
      border-color: #dc3545;
      color: #721c24;
    }

    .question-item.correct::after {
      content: '✓';
      position: absolute;
      top: 2px;
      right: 4px;
      font-size: 12px;
      color: #28a745;
    }

    .question-item.incorrect::after {
      content: '✗';
      position: absolute;
      top: 2px;
      right: 4px;
      font-size: 12px;
      color: #dc3545;
    }

    .question-item.active.answered {
      background: #667eea;
      color: white;
    }

    .question-item.active.answered::after {
      color: white;
    }

    .progress-info {
      margin-top: auto;
      padding-top: 20px;
      border-top: 2px solid #e0e0e0;
      text-align: center;
    }

    .progress-text {
      font-size: 14px;
      color: #666;
      margin-bottom: 10px;
    }

    .progress-bar {
      width: 100%;
      height: 8px;
      background: #e0e0e0;
      border-radius: 4px;
      overflow: hidden;
      margin-bottom: 15px;
    }

    .progress-fill {
      height: 100%;
      background: #667eea;
      transition: width 0.3s ease;
    }

    /* Modal kết quả */
    .modal-overlay {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0,0,0,0.7);
      z-index: 1000;
      align-items: center;
      justify-content: center;
    }

    .modal-overlay.show {
      display: flex;
    }

    .modal-content {
      background: white;
      border-radius: 20px;
      padding: 40px;
      max-width: 500px;
      width: 90%;
      text-align: center;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
    }

    .modal-icon {
      font-size: 64px;
      margin-bottom: 20px;
    }

    .modal-title {
      font-size: 28px;
      font-weight: 700;
      color: #333;
      margin-bottom: 15px;
    }

    .modal-score {
      font-size: 48px;
      font-weight: 700;
      color: #667eea;
      margin-bottom: 20px;
    }

    .modal-message {
      font-size: 16px;
      color: #666;
      margin-bottom: 30px;
    }

    .modal-buttons {
      display: flex;
      gap: 15px;
    }

    .btn-modal {
      flex: 1;
      padding: 12px 24px;
      font-size: 16px;
      font-weight: 600;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .btn-review {
      background: #667eea;
      color: white;
    }

    .btn-review:hover {
      background: #5568d3;
    }

    .btn-restart {
      background: white;
      color: #667eea;
      border: 2px solid #667eea;
    }

    .btn-restart:hover {
      background: #667eea;
      color: white;
    }

    /* Responsive */
    @media (max-width: 768px) {
      .quiz-container {
        flex-direction: column;
        height: 95%;
      }

      .question-section {
        padding: 20px;
      }

      .sidebar {
        width: 100%;
        max-height: 200px;
        border-left: none;
        border-top: 1px solid #e0e0e0;
      }

      .question-list {
        grid-template-columns: repeat(5, 1fr);
      }

      .progress-info {
        display: none;
      }
    }
  </style>
  <style>@view-transition { navigation: auto; }</style>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
  <script src="/_sdk/element_sdk.js" type="text/javascript"></script>
  <script src="https://cdn.tailwindcss.com" type="text/javascript"></script>
 </head>
 <body>
  <div class="app-wrapper">
   <div class="quiz-container"><!-- Phần câu hỏi bên trái -->
    <div class="question-section">
     <header class="quiz-header">
      <h1>🎯 Bài Ôn Luyện Trắc Nghiệm</h1>
      <p>Chọn đáp án đúng và điều hướng qua các câu hỏi</p>
     </header>
     <div class="question-display" id="question-display"><!-- Câu hỏi sẽ được hiển thị ở đây -->
     </div>
     <div class="navigation-buttons"><button id="prev-btn" class="btn btn-nav"> ← Câu trước </button> <button id="next-btn" class="btn btn-nav"> Câu sau → </button>
     </div><button id="submit-btn" class="btn btn-submit" style="margin-top: 15px; display: none;"> 🎉 Nộp bài và xem kết quả </button>
    </div><!-- Sidebar danh sách câu hỏi bên phải -->
    <aside class="sidebar">
     <div class="sidebar-header">
      Danh sách câu hỏi
     </div>
     <div class="question-list" id="question-list"><!-- Danh sách câu hỏi sẽ ��ược tạo ở đây -->
     </div>
     <div class="progress-info">
      <div class="progress-text" id="progress-text">
       Đã làm: 0/10
      </div>
      <div class="progress-bar">
       <div class="progress-fill" id="progress-fill" style="width: 0%"></div>
      </div>
     </div>
    </aside>
   </div>
  </div><!-- Modal kết quả -->
  <div class="modal-overlay" id="result-modal">
   <div class="modal-content">
    <div class="modal-icon" id="modal-icon">
     🎉
    </div>
    <div class="modal-title">
     Hoàn thành bài quiz!
    </div>
    <div class="modal-score" id="modal-score">
     0/10
    </div>
    <div class="modal-message" id="modal-message">
     Chúc mừng bạn đã hoàn thành!
    </div>
    <div class="modal-buttons"><button class="btn-modal btn-review" id="review-btn">Xem lại đáp án</button> <button class="btn-modal btn-restart" id="restart-btn">Làm lại</button>
    </div>
   </div>
  </div>
  <script>
    // ===== DANH SÁCH CÂU HỎI =====
    const questionsData = [
      {
        id: 1,
        question: "HTML là viết tắt của?",
        options: [
          { value: "A", text: "Hyper Trainer Marking Language" },
          { value: "B", text: "Hyper Text Markup Language" },
          { value: "C", text: "Home Tool Markup Language" },
          { value: "D", text: "Hyperlinking Text Management Language" }
        ],
        correctAnswer: "B"
      },
      {
        id: 2,
        question: "CSS dùng để làm gì?",
        options: [
          { value: "A", text: "Xử lý dữ liệu phía server" },
          { value: "B", text: "Mô tả cách trình bày giao diện web" },
          { value: "C", text: "Lưu trữ dữ liệu người dùng" },
          { value: "D", text: "Định nghĩa cấu trúc tài liệu" }
        ],
        correctAnswer: "B"
      },
      {
        id: 3,
        question: "JavaScript thường ch��y ở đâu?",
        options: [
          { value: "A", text: "Trình duyệt hoặc môi trường như Node.js" },
          { value: "B", text: "Chỉ trên hệ điều hành Linux" },
          { value: "C", text: "Chỉ trên máy chủ cơ sở dữ liệu" },
          { value: "D", text: "Không ở đâu cả, chỉ là lý thuyết" }
        ],
        correctAnswer: "A"
      },
      {
        id: 4,
        question: "Thẻ nào dùng để tạo liên kết trong HTML?",
        options: [
          { value: "A", text: "<link>" },
          { value: "B", text: "<a>" },
          { value: "C", text: "<href>" },
          { value: "D", text: "<url>" }
        ],
        correctAnswer: "B"
      },
      {
        id: 5,
        question: "JSON là viết tắt của?",
        options: [
          { value: "A", text: "JavaScript Object Notation" },
          { value: "B", text: "Java Standard Object Notation" },
          { value: "C", text: "JavaScript Online Network" },
          { value: "D", text: "Java Syntax Object Name" }
        ],
        correctAnswer: "A"
      },
      {
        id: 6,
        question: "Thuộc tính nào dùng để thay đổi màu chữ trong CSS?",
        options: [
          { value: "A", text: "font-color" },
          { value: "B", text: "text-color" },
          { value: "C", text: "color" },
          { value: "D", text: "text-style" }
        ],
        correctAnswer: "C"
      },
      {
        id: 7,
        question: "Phương thức nào dùng để chọn phần tử theo ID trong JavaScript?",
        options: [
          { value: "A", text: "getElementByClass()" },
          { value: "B", text: "getElementById()" },
          { value: "C", text: "querySelector()" },
          { value: "D", text: "selectElement()" }
        ],
        correctAnswer: "B"
      },
      {
        id: 8,
        question: "HTTP là viết tắt của?",
        options: [
          { value: "A", text: "HyperText Transfer Protocol" },
          { value: "B", text: "High Transfer Text Protocol" },
          { value: "C", text: "Hyperlink Transfer Text Protocol" },
          { value: "D", text: "Home Transfer Text Protocol" }
        ],
        correctAnswer: "A"
      },
      {
        id: 9,
        question: "Đâu là kiểu dữ liệu trong JavaScript?",
        options: [
          { value: "A", text: "String, Number, Boolean" },
          { value: "B", text: "Integer, Float, Char" },
          { value: "C", text: "Byte, Short, Long" },
          { value: "D", text: "Varchar, Text, Blob" }
        ],
        correctAnswer: "A"
      },
      {
        id: 10,
        question: "Thẻ nào dùng để nhúng JavaScript vào HTML?",
        options: [
          { value: "A", text: "<js>" },
          { value: "B", text: "<javascript>" },
          { value: "C", text: "<script>" },
          { value: "D", text: "<code>" }
        ],
        correctAnswer: "C"
      }
    ];

    // ===== BIẾN TRẠNG THÁI =====
    let currentQuestionIndex = 0;
    let userAnswers = {}; // Lưu đáp án người dùng {questionId: answer}
    let isSubmitted = false;

    // ===== H��M RENDER DANH SÁCH CÂU HỎI (SIDEBAR) =====
    function renderQuestionList() {
      const listContainer = document.getElementById('question-list');
      listContainer.innerHTML = '';

      questionsData.forEach((q, index) => {
        const item = document.createElement('div');
        item.className = 'question-item';
        item.textContent = index + 1;
        item.dataset.index = index;

        if (index === currentQuestionIndex) {
          item.classList.add('active');
        }

        if (isSubmitted && userAnswers[q.id]) {
          // Sau khi nộp bài, hiển thị đúng/sai
          if (userAnswers[q.id] === q.correctAnswer) {
            item.classList.add('correct');
          } else {
            item.classList.add('incorrect');
          }
        } else if (userAnswers[q.id]) {
          // Trước khi nộp bài, chỉ hiển thị đã làm
          item.classList.add('answered');
        }

        item.addEventListener('click', () => {
          goToQuestion(index);
        });

        listContainer.appendChild(item);
      });

      updateProgress();
    }

    // ===== HÀM RENDER CÂU HỎI HIỆN TẠI =====
    function renderCurrentQuestion() {
      const question = questionsData[currentQuestionIndex];
      const displayContainer = document.getElementById('question-display');
      const userAnswer = userAnswers[question.id];
      const isCorrect = userAnswer === question.correctAnswer;

      displayContainer.innerHTML = `
        <div class="question-card">
          <div class="question-number">Câu ${currentQuestionIndex + 1}/${questionsData.length}</div>
          <div class="question-title">${question.question}</div>
          <ul class="options-list" id="options-list">
            ${question.options.map(opt => {
              let optionClass = 'option-label';
              let statusIcon = '';
              
              if (isSubmitted) {
                // Hiển thị đáp án đúng
                if (opt.value === question.correctAnswer) {
                  optionClass += ' option-correct';
                  statusIcon = '<span style="margin-left: auto; color: #28a745; font-weight: bold;">✓ Đáp án đúng</span>';
                }
                // Hiển thị đáp án sai của người dùng
                if (userAnswer === opt.value && opt.value !== question.correctAnswer) {
                  optionClass += ' option-incorrect';
                  statusIcon = '<span style="margin-left: auto; color: #dc3545; font-weight: bold;">✗ Sai</span>';
                }
              }
              
              return `
                <li class="option-item">
                  <label class="${optionClass}">
                    <input 
                      type="radio" 
                      name="question-${question.id}" 
                      value="${opt.value}"
                      ${userAnswers[question.id] === opt.value ? 'checked' : ''}
                      ${isSubmitted ? 'disabled' : ''}
                    />
                    <span class="option-text">${opt.value}. ${opt.text}</span>
                    ${statusIcon}
                  </label>
                </li>
              `;
            }).join('')}
          </ul>
          ${isSubmitted && userAnswer ? `
            <div class="answer-feedback ${isCorrect ? 'feedback-correct' : 'feedback-incorrect'}">
              ${isCorrect 
                ? '🎉 <strong>Chính xác!</strong> Bạn đã chọn đúng đáp án.' 
                : `😔 <strong>Chưa đúng.</strong> Đáp án đúng là: <strong>${question.correctAnswer}</strong>`
              }
            </div>
          ` : ''}
        </div>
      `;

      // Lắng nghe sự kiện chọn đáp án
      const radios = displayContainer.querySelectorAll('input[type="radio"]');
      radios.forEach(radio => {
        radio.addEventListener('change', (e) => {
          userAnswers[question.id] = e.target.value;
          renderQuestionList();
          updateNavigationButtons();
          
          // Tự động chuyển sang câu tiếp theo sau 400ms
          if (!isSubmitted && currentQuestionIndex < questionsData.length - 1) {
            setTimeout(() => {
              goToQuestion(currentQuestionIndex + 1);
            }, 400);
          }
        });
      });

      updateNavigationButtons();
    }

    // ===== HÀM CẬP NHẬT NÚT ĐIỀU HƯỚNG =====
    function updateNavigationButtons() {
      const prevBtn = document.getElementById('prev-btn');
      const nextBtn = document.getElementById('next-btn');
      const submitBtn = document.getElementById('submit-btn');

      prevBtn.disabled = currentQuestionIndex === 0;
      nextBtn.disabled = currentQuestionIndex === questionsData.length - 1;

      // Hiện nút nộp bài nếu đã làm hết
      const answeredCount = Object.keys(userAnswers).length;
      if (answeredCount === questionsData.length) {
        submitBtn.style.display = 'block';
      } else {
        submitBtn.style.display = 'none';
      }
    }

    // ===== HÀM CẬP NHẬT TIẾN TRÌNH =====
    function updateProgress() {
      const answeredCount = Object.keys(userAnswers).length;
      const total = questionsData.length;
      const percentage = (answeredCount / total) * 100;

      document.getElementById('progress-text').textContent = `Đã làm: ${answeredCount}/${total}`;
      document.getElementById('progress-fill').style.width = percentage + '%';
    }

    // ===== HÀM CHUYỂN ĐẾN CÂU HỎI =====
    function goToQuestion(index) {
      const displayContainer = document.getElementById('question-display');
      const currentCard = displayContainer.querySelector('.question-card');
      
      if (currentCard) {
        currentCard.classList.add('fade-out');
        setTimeout(() => {
          currentQuestionIndex = index;
          renderCurrentQuestion();
          renderQuestionList();
        }, 300);
      } else {
        currentQuestionIndex = index;
        renderCurrentQuestion();
        renderQuestionList();
      }
    }

    // ===== HÀM CHẤM ĐIỂM =====
    function calculateScore() {
      let correctCount = 0;
      questionsData.forEach(q => {
        if (userAnswers[q.id] === q.correctAnswer) {
          correctCount++;
        }
      });
      return correctCount;
    }

    // ===== HÀM HIỂN THỊ KẾT QUẢ =====
    function showResult() {
      const score = calculateScore();
      const total = questionsData.length;
      const percentage = Math.round((score / total) * 100);

      const modal = document.getElementById('result-modal');
      const modalIcon = document.getElementById('modal-icon');
      const modalScore = document.getElementById('modal-score');
      const modalMessage = document.getElementById('modal-message');

      modalScore.textContent = `${score}/${total}`;

      if (percentage >= 80) {
        modalIcon.textContent = '🎉';
        modalMessage.textContent = 'Xuất sắc! Bạn đã làm rất tốt!';
      } else if (percentage >= 50) {
        modalIcon.textContent = '👍';
        modalMessage.textContent = 'Khá tốt! Hãy cố gắng thêm nhé!';
      } else {
        modalIcon.textContent = '💪';
        modalMessage.textContent = 'Đừng nản lòng! Hãy thử lại nhé!';
      }

      modal.classList.add('show');
      isSubmitted = true;
    }

    // ===== HÀM RESET BÀI QUIZ =====
    function resetQuiz() {
      currentQuestionIndex = 0;
      userAnswers = {};
      isSubmitted = false;
      document.getElementById('result-modal').classList.remove('show');
      renderCurrentQuestion();
      renderQuestionList();
    }

    // ===== KHỞI TẠO =====
    renderQuestionList();
    renderCurrentQuestion();

    // ===== GẮN SỰ KIỆN =====
    document.getElementById('prev-btn').addEventListener('click', () => {
      if (currentQuestionIndex > 0) {
        goToQuestion(currentQuestionIndex - 1);
      }
    });

    document.getElementById('next-btn').addEventListener('click', () => {
      if (currentQuestionIndex < questionsData.length - 1) {
        goToQuestion(currentQuestionIndex + 1);
      }
    });

    document.getElementById('submit-btn').addEventListener('click', () => {
      showResult();
    });

    document.getElementById('review-btn').addEventListener('click', () => {
      document.getElementById('result-modal').classList.remove('show');
      goToQuestion(0);
    });

    document.getElementById('restart-btn').addEventListener('click', () => {
      resetQuiz();
    });
  </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'9a7173da4539ddc9',t:'MTc2NDU4MDIzOC4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>